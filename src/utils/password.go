package utils

// 注意：ValidatePassword 和 ValidatePhoneWithRegex 已被 Domain 层替代
// - ValidatePassword → value_objects.NewStrictPassword (Step 43)
// - ValidatePhoneWithRegex → value_objects.NewPhone (Step 26)
